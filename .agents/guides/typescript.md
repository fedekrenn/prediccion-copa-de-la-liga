# TypeScript

- Evitar `any` y `unknown`.
- Preferir inferencia cuando el tipo pueda derivarse con claridad desde el codigo existente.
- Si el tipo no puede derivarse con seguridad despues de revisar el repo, pedir aclaracion antes de continuar.
